# 상시 디자인 정책 (Design Policy)

> **무엇인가**: 한 번 확정한 뒤로 **모든 후속 작업에 계속 적용되는** 게임 디자인 결정들이다. 개별 기능 스펙(`game_design.md`)과 달리, 여기 있는 항목은 *새 기능을 만들 때마다 다시 지켜야 하는 제약*이다.
>
> 원본 위치: 구 `docs/agents/subagent-handoff.md` §1.5 + `game_design.md` Phase 15 비범위 결정.
> ⚖️ = 제작자 확정. 날짜는 확정 시점.

---

## 1. 입력·플랫폼

### ⚖️ PC/모바일 UI 레이아웃 분기 금지 — 단일 고정 레이아웃 (2026-07-15)

`IsMobilePlatform()` 등으로 런타임에 배치를 가르지 않는다. 전 플랫폼 공통 고정 레이아웃 하나만 둔다.

- 터치 타겟은 상시 **≥88px** 지향 (T62 스킬바 선례).
- 이 정책 전환으로 T52의 플랫폼 분기부가 T62로 대체됐다. 클릭 시전·이름 숨김·툴팁 기능 자체는 존치.

### ⚖️ 월드 클릭/터치 상호작용 금지 (2026-07-15, T59)

상호작용 수단은 **`F` 키(PC) / `BtnInteract`(모바일)만**이다. 사유 = UI 창과 클릭이 겹쳐 조작이 꼬였다.

- 월드 엔티티에 `TouchEvent` 상호작용을 부착하지 않는다.
- 신규 인터랙터블은 **이중 연결** 필수:
  1. 자체 `F` 핸들러 (`KeyDownEvent` — `MerchantInteract` 패턴)
  2. `InteractRequestEvent` 구독 (모바일 브리지)
- 하나만 붙이면 한쪽 플랫폼에서 조용히 조작 불능이 된다.

### 상호작용 판정 = 조준 셀 (T67 · T82)

"근처에 있으면 발동"이 아니라 **바라보는 조준 셀** 단일 기준이다.

- 판정은 `IsAimTarget` 헬퍼 단일 정의를 쓴다. 분산 핸들러가 각자 거리 판정을 두지 않는다.
- footprint는 **Trigger 월드 AABB ∩ 조준 셀 사각형**으로 자동 정합된다(T82). Trigger가 없는 대상만 기존 footprint 폴백.
- 🧭 트리거형(포탈 발밑)만 예외.

---

## 2. 톤 — "아늑한 생활"

이 게임의 톤은 **아늑한 생활 시뮬레이션**이다. 플레이어를 압박하는 페널티형 시스템은 채택하지 않는다.

| 결정 | 내용 |
|---|---|
| ⚖️ **날씨는 보너스만** (2026-07-11) | 비 = 작물 성장 가속 + 낚시 입질↑. **페널티 없음** |
| ⚖️ **영지 평화 원칙** | 개인 영지 안에서는 전투·피격이 없다. 몬스터 관련 장치는 사냥터·보스맵 전용 |
| ⚖️ **허기 시스템 기각** | 페널티형이라 톤과 충돌 |
| ⚖️ **도감 보상 = 최초 발견 즉시 자동 지급** (2026-07-13) | 마일스톤 누적형 미채택. Rarity 차등, `DexRewardDataSet` |

## 3. 복잡도 억제

### ⚖️ 스킬트리 단순성 가드라인 (2026-07-15 — "복잡한 구조 거부")

- **단일 부모**
- **같은 열 바로 위 행만** 연결
- **3행 × 3열 상한**

상세: [design/skill-tree-plan.md](./design/skill-tree-plan.md) §8.3. 전직 확장 계약은 동 문서 §7.

### ⚖️ Trigger 박스 1개가 3역할 겸임 — 현행 유지 (2026-07-25)

`TriggerComponent` 박스 하나가 **통행 차단 + 상호작용 범위 + walk-behind 페이드 + Y정렬 접지선**을 동시에 담당한다. 분리하지 않는다 — 단일 데이터 소스 이점 우선.

- 트레이드오프는 **의도된 것**: 충돌 박스를 튜닝하면 나머지 셋도 함께 변한다.
- Play에서 실제 불편이 확인되면 그때 분리를 검토한다.
- 박스를 만질 때의 산정 규칙은 [pitfalls.md 규칙 17](./pitfalls.md#규칙-17-trigger배치는-스프라이트-실루엣-정합이-1순위다).

### ⚖️ 기믹 도구 중첩 = `item_dataset.MaxStack` (2026-08-14)

기본 도구는 인스턴스별 고유 키(한 슬롯에 1개)다. **기믹 무기**만 `MaxStack>1`로 자원처럼 한 슬롯에 중첩한다. 이름 분기를 두지 않는다 (`if name == "Hand Axe"` 금지). 현재 대상: `Hand Axe`(주먹도끼, MaxStack=99) + 스킬 `hand_axe_throw`(1개 소모 투사체).

주먹도끼의 **아이템·레시피 설명은 벌목 도구**다. 던지기는 툴팁에 적지 않고, 퀘스트명(`왠지 던질 수 있을것 같습니다..`)과 스킬로만 드러내는 기믹이다. 시전은 `RequireEquippedItem=Hand Axe` — **들고 있을 때만**, 스택 1개 소모.

### ⚖️ 전투 데미지 세 갈래 (2026-08-16)

채집 `ToolPower`를 전투에 곱하지 않는다. `item_dataset.Attack`이 무기 공격력이다.

| 갈래 | 식 | 대상 |
|---|---|---|
| 평타 | CharAtk + Attack | Ctrl 근접 |
| Physical | (평타 + SkillAttack) × 실효배율 | 파워 스트라이크, 슬래시 블러스트 |
| Magic | (SkillPower + MagicAtk + MagicAttack + SkillAttack) × 실효배율 | 매직 클로, 플래시 점프 |
| Throw | (SkillPower + CharAtk + Attack + SkillAttack) × 실효배율 | 주먹도끼 던지기 (`RequireEquippedItem`) |

CharAtk = 8 + 버프. MagicAtk = 0 + MagicPower 버프. 스탯 투자는 `GetCharacterAttack()` / `GetMagicAttack()`. 아이템 `MagicAttack`·`SkillAttack`은 지금은 0(지팡이·스킬 무기용 칸). 던지기 MaxLevel 5 · DamagePerLevel 0.3.

툴팁: 아이템은 맛 설명 + `T티어 종류 · 채집 +N` / `무기 공격력` / `마법 공격력` / `스킬 공격력`(0은 숨김). 스킬은 `물리 스킬`·`마법 스킬`·`투척 스킬`·`패시브`.

---

## 4. 비범위 (⚖️ 2026-07-11 확정 — 되살릴 때는 명시 결정 필요)

| 항목 | 상태 |
|---|---|
| 영지 방문 · 랭킹 | 후속 Phase 후보 |
| 계절 테마 | 아트 방향 확정 후 |
| **허기 시스템** | **기각** (톤 충돌) |
| 가축 번식 · 펫 전투 · 날씨↔몬스터 연동 | 비범위 |
| ~~낚시 게이지 미니게임 고도화~~ | ⚖️ 2026-07-16 해제 → T64 릴링 v2로 구현됨 |

---

## 5. 세이브·캐릭터·대화 (⚖️ 2026-08-08 · UX 보강 2026-08-09)

| 결정 | 내용 |
|---|---|
| 슬롯 수 | **5 고정** |
| 닉네임 | 슬롯별 캐릭터 닉네임. **글로벌+계정 내 중복 생성 금지** |
| 주간 낚시왕 | **캐릭터(슬롯) 단위** (`CharacterId = userId_sN`) |
| F 대화 | **메이플식 하단 대화창** (말풍선 아님). 퀘스트 수락/거절. HUD·퀵슬롯 가림 허용 |
| 퀘스트 로그 | ⚖️ 2026-08-14 **A안 좌목록+우상세**. HUD 트래커 클릭. 수락/거절은 하단 대화창. 크롬=도감 탭(180×44)+제작대 종이 속지+잉크 본문 |
| 스토리 톤 | ⚖️ 2026-08-14 개정 **코지 기본 + 이원 미스터리** (긍정: 푸른 빛 / 메인 빌런: 잠식의 원인). 구 "죽음·공포·정치 전면 금지"는 해제 — 잔혹·고어, 절망 전개, 현실 정치 풍자만 회피. 몬스터=잠식된 이웃 생물, 전투="퇴치", 사냥 드롭=연구·발전 재료. 원문: [design/story/story-bible.md](./design/story/story-bible.md) §0 |
| 타이틀 UX | 키아트 + **새로하기 / 이어하기 / 종료하기**만. 슬롯·외모 커스텀은 하위 화면 |
| 슬롯 외형 | 기본=**계정 아바타 유지** (`useAccount`). 선택 시만 커스텀 파츠 + `UseCustomEquipOnly` |
| 메인메뉴 가독성 | 주요 텍스트 뒤 반투명 플레이트 + 메뉴 버튼 반투명 채움 |
| 버튼 호버 | ⚖️ 2026-08-14 `ButtonComponent` ColorTint. 평소=작성 스프라이트색(Normal 백색). 호버=따뜻한 골드. 누름=어둡게 |
| 버튼 SFX | ⚖️ 2026-08-14 호버 `159bf70a…` / 클릭 `972843e7…`. 메인메뉴 `BindBtn`에서 재생 |
| 타이틀 키아트 | ⚖️ 2026-08-14 보이는 장은 `Bg/Art`(AspectOnly cover). `Bg`는 Mask 껍데기(alpha 0). 원본 1376×768을 1920×1080 Stretch(None)에 넣으면 비균등 업스케일로 열화됨 |
| **팝업 크롬 2계열** | ⚖️ 2026-08-14 확정. **종이 큰창**(인벤 기준 — 인벤·제작·도감·퀘스트·캐릭터): 나무 프레임(25e9e895) **루트 크기 풀블리드** + 종이(c24adedc) 속지 + 제목 (0,-28) 400×48 fs32 크림(0.9,0.9,0.8) / **나무 카드**(의뢰 기준 — 의뢰·연구·상점·워프·권한·상자·화로·스킬): Bg 4fea64a3 (0.2,0.1,0.1) + TopBar 카드폭×56 (0.1,0.1,0.1) + 골드 AccentLine 3px + 제목 (0,-8) fs26 크림(1,0.9,0.7). **닫기 = 전 창 공통 흰 X 스프라이트(221e0368) 88×88 최상단 z(dOrd 20)** — 종이창 (-35,-35)·카드 (-12,-12). 새 팝업은 이 두 계열 중 하나로만. **워프 예외**: 제목·닫기 앵커는 중앙 고정 — `UIWarpController`가 중앙 좌표계로 런타임 재배치(`totalH/2-40`). 제목·본문에 이모지 금지(글리프 fallback) |

---

## 6. 표시 언어 — 한글 기준 (⚖️ 2026-08-14)

플레이어에게 보이는 문구는 **한글**이 기본이다. 저장·레시피·상점 키는 영문 `Name`을 유지한다.

| 구분 | 규칙 |
|---|---|
| 아이템 식별자 | `item_dataset.Name` (Wood, Stone Pickaxe, …). 세이브·제작·상점·의뢰 키. **한글로 바꾸지 않는다** |
| 아이템 표시명 | `item_dataset.DisplayName`. UI는 `GetItemDisplayName` / `pcall(GetItem("DisplayName"))` |
| 레시피 | `RecipeName`·`Ing*Name` = 아이템 `Name`. `Desc`·`UnlockHint`만 한글 |
| 카테고리/탭 키 | `tool`/`All`/`Resources` 등 내부 키 유지. 칩·탭 **글자만** 한글 |
| UI 작성 문구 | `.ui` 타이틀·탭·버튼·HUD 라벨은 한글 |

---

## 7. 카메라·비주얼 스케일 및 조망 (⚖️ 2026-08-21)

| 구분 | 규칙 |
|---|---|
| 물리·좌표계 | `1.0 × 1.0` 표준 타일 그리드 유지. 물리 좌표계를 분수 단위로 쪼개지 않는다 |
| 기본 카메라 줌 | `CameraComponent.ZoomRatio = 33.0` (기존 66 대비 0.5배 줌아웃으로 2배 넓은 시야 확보) |
| 캐릭터·오브젝트 스케일 | 플레이어(2.8배), 몬스터/NPC/자연자원/가구/프롭/드롭아이템(2.0배), 점유 영역(나무/바위 등 2×2, 4×4 점유) |
| 이동속도 | 기본 플레이어 이동속도 `7.2` (기존 3.6 대비 2배 가속), 몬스터/동물 `InputSpeed` 2배 가속 |
| 전경 조망 모드 (Eagle-Eye) | `Tab` 키 또는 우측 상단 미니맵 클릭 시 맵 맞춤형 초광각 동적 줌(`ZoomRatio 7.5~10.0`) 및 맵 중심 `(0, 0)` 자동 정렬. 외곽 비네팅 마스크 표시. 이동/Tab 재입력 시 즉시 복귀 |

---

## 관련 문서

- 전체 게임 설계: [../game_design.md](../game_design.md)
- 스킬트리 설계: [design/skill-tree-plan.md](./design/skill-tree-plan.md)
- Phase 15 살아있는 월드 기획: [design/phase15-living-world.md](./design/phase15-living-world.md)
- 아트워크 명세: [design/artwork-spec.md](./design/artwork-spec.md)
- 함정 사전: [pitfalls.md](./pitfalls.md)

