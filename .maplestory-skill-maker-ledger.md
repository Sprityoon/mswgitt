# maplestory-skill-maker 진행 원장 — 플레이어 스킬 이펙트 점검/수정 (2026-09-09)

요청: 플레이어 스킬 이펙트 감사에서 나온 5건 수정 + 이펙트 종류 컨셉 점검.
모드: **Player attack skill (modify)** — 연출/데이터 한정. 판정·데미지·사망 파이프라인 변경 없음.

## ⚠ 아키텍처 발산 선언

이 스킬 패키지의 레퍼런스(`architecture/datasets.md`, `architecture/divergences.md` 등)는
`SkillCatalogLogic` / `AttackSkillData` / `CollisionSimulator:OverlapBoxAll` + `TakeDamage` 구조를 전제한다.
**이 프로젝트는 그 구조가 아니다.** 실제 소유자는 아래 역할 지도와 같으며, 판정은 네이티브
`AttackComponent:AttackFast(shape, nil, CollisionGroups.HitBox)` 경로를 쓴다(그리고 정상 동작한다).

레퍼런스의 구체 파일명은 SKILL.md 규정상 "current instantiation / example"이므로 역할로만 매핑했고,
파이프라인 교체는 **AGENTS.md §7 범위 밖 리팩터링 금지**에 따라 하지 않았다. 사용자 요청 범위만 수정.

## 역할 지도 (current instantiation)

| 역할 | 이 프로젝트의 소유자 |
|---|---|
| 공격 데이터 | `RootDesk/MyDesk/Player/DataSets/SkillDataSet.csv` (+ `.userdataset`) |
| 카탈로그/디스패치 | `PlayerController.mlua` — `ExecuteSkill` (시전 시 셀 직접 읽음), `ExecuteAreaDamageSkill` / `ExecuteDashSkill` / `ExecuteProjectileSkill` |
| 판정 | `PlayerCombat` (extends AttackComponent) — `AttackFast` + `PendingDamage` |
| 시전 연출 | `PlayerController.MulticastPlayEffectEx` → `_EffectService:PlayEffect` (3단 폴백) |
| 피격 연출 | `Monster.mlua` `HandleHitEvent` → `pc:MulticastPlayEffectEx`; RUID 전달은 `PlayerCombat.PendingHitEffectRUID` |
| 투사체 | `Projectile.mlua` + `Player/Models/Projectile_Fireball.model` |
| 사운드 | `PlayerController.MulticastPlaySkillSound` |

## 원장

| ID | 요구사항 | 확정 결정 | 소유자 | 구현 위치 | 검증 시나리오/증거 | 상태 |
|---|---|---|---|---|---|---|
| `FX-01` | 매직 클로 탄환 비가시 | `ProjectileRUID` 공란 시 `EffectRUID` 폴백, 둘 다 비면 모델 기본값 유지(빈 문자열 대입 금지) | `ExecuteProjectileSkill` | `PlayerController.mlua` | 정적: 빈 대입 경로 제거 확인 / 런타임: 탄환 육안 | 정적 PASS · 런타임 BLOCKED |
| `FX-02` | 디버그 텔레메트리 제거 | `#region agent log` 블록(로컬 HTTP POST 포함) 삭제 | `ExecuteProjectileSkill` | `PlayerController.mlua` | 정적: `DBG985ecc` / `7370` 잔여 0건 | PASS |
| `FX-03` | 히트 이펙트가 `EffectScale` 무시 | `HitEffectScale` 컬럼 신설 + `PendingHitEffectScale` 전달, flipX는 공격자 `LastDirectionX` | `PlayerController` / `PlayerCombat` / `Projectile` / `Monster` | 4개 `.mlua` + CSV | 정적: 전달 체인 6지점 grep 확인 완료 / 런타임: 스킬별 히트 FX 크기 | 정적 PASS · 런타임 BLOCKED |
| `FX-04` | 주먹도끼 히트 이펙트 없음 | `HitEffectRUID` = `34cee742…`(물리 임팩트, 파워 스트라이크와 공유) | SkillDataSet | CSV | 런타임: 타격 시 FX 출현 | 런타임 BLOCKED |
| `FX-05` | 슬래시 블러스트 이펙트 < 판정 | 이펙트 교체(`4a968bf7…` 395×389 지면 폭발) + `EffectScale` 2.0 → 약 7.9×7.8 유닛으로 8×8 판정 커버 | SkillDataSet | CSV | 런타임: 판정 박스와 육안 정합 | 런타임 BLOCKED |
| `FX-06` | 컨셉 불일치(주먹도끼) | 탄환/아이콘이 `돌멩이`/`돌 조각` → 손도끼 스프라이트 `2e0751aa…` | SkillDataSet | CSV | 런타임: 던지는 물체가 도끼로 보임 | 런타임 BLOCKED |
| `FX-07` | 컨셉 불일치(매직 클로) | 탄환에 발톱 클립 `983a41e8…` 지정. 시전 이펙트(파란 조준 원)는 **취향 영역이라 미변경**, 사용자 선택으로 남김 | SkillDataSet | CSV | 런타임 육안 | 런타임 BLOCKED |
| `FX-08` | 모델 `SpriteRUID` 공란 | `Projectile_Fireball.model` 기본 스프라이트 지정(빈 RUID = 투명, 8대 규칙 3) | ModelBuilder | `Projectile_Fireball.model` | 정적: 값 확인 | PASS |

## 게이트 상태 (verification/non-negotiable-presentation-gates.md)

| 게이트 | 적용 | 상태 | 사유 |
|---|---|---|---|
| Gate P (플레이어 시전 연출) | 적용 | **BLOCKED** | 이번 변경은 애니메이션/캐스트 제어 미변경이나, `P0`–`P12` 런타임 하네스 증거는 Play 권한이 이번 턴에 없어 수집 불가 |
| Gate M (몬스터 피격/사망 연출) | 적용 | **BLOCKED** | 히트 이펙트 배율/방향이 바뀌므로 `H`/`E` 행 재확인 필요. 런타임 증거 없음 |
| Gate B (부트스트랩) | 미적용 | N/A | 공격 인프라가 이미 존재하고 완성 상태 |

**완료 선언 불가**: 위 두 게이트가 BLOCKED이므로 "완료"가 아니라 "구현 완료 · 런타임 검증 보류"로 보고한다.
(AGENTS.md §8 — Play는 제작자 전담, 이번 턴 지시 없음)

## 실행 중 발생한 사고

- `Projectile.Initialize` 인자를 5→6개로 늘렸다가 빌드 `LEA-1102`. 에러가 **옛 5개 시그니처**를 찍어
  오진하기 쉬웠다. 인자 수를 되돌리고 `p.HitEffectScale = hs` 프로퍼티 주입으로 전환해 Error 0 복구.
  → [pitfalls.md 규칙 46](docs/pitfalls.md) 신설.

## 최종 빌드 검증

`maker_refresh_workspace` status ok · `maker_logs(kind="build")` `dateTime 2026-09-09T16:04:55`
(이번 refresh 시각과 일치 — 규칙 22 대조 완료) · **637건 전량 Info, Error 0 / Warning 0**.
